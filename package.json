{
  "name": "efg-allendorf-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test tests/*.test.js",
    "check": "python3 scripts/check-site.py && npm test"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
